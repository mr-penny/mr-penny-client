import { MatSnackBarConfig } from '@angular/material';

export const defaultSnackbarOptions: MatSnackBarConfig = {
  direction: 'ltr',
  duration: 3000,
  horizontalPosition: 'center',
  verticalPosition: 'bottom',
};
